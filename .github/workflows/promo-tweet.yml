name: Promo Tweet

on:
  # X凍結により停止中（2026-05-11）
  # schedule:
  #   # 火・金 12:47 JST (UTC 03:47)
  #   - cron: '47 3 * * 2,5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  promo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Disabled (X凍結により停止中)
        run: echo "X凍結により停止中（2026-05-11）"

      # - name: Install dependencies
      #   run: npm ci
      #
      # - name: Generate and post promo tweet
      #   env:
      #     ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      #     X_API_KEY: ${{ secrets.X_API_KEY }}
      #     X_API_SECRET: ${{ secrets.X_API_SECRET }}
      #     X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
      #     X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
      #   run: node promo-tweet.js
      #
      # - name: Save tweet history
      #   run: |
      #     git config user.name "github-actions[bot]"
      #     git config user.email "github-actions[bot]@users.noreply.github.com"
      #     git add tweet-history.txt
      #     if ! git diff --staged --quiet; then
      #       git commit -m "Update promo tweet history"
      #       git pull --rebase
      #       git push
      #     fi
