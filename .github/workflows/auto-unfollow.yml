name: Auto Unfollow

on:
  # X凍結により停止中（2026-05-11）
  # schedule:
  #   # 毎日 15:11 JST (UTC 06:11)
  #   - cron: '11 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  auto-unfollow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Disabled (X凍結により停止中)
        run: echo "X凍結により停止中（2026-05-11）"

      # - name: Install dependencies
      #   run: npm ci
      #
      # - name: Check and unfollow
      #   env:
      #     X_API_KEY: ${{ secrets.X_API_KEY }}
      #     X_API_SECRET: ${{ secrets.X_API_SECRET }}
      #     X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
      #     X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
      #   run: node auto-unfollow.js
      #
      # - name: Save followed users
      #   run: |
      #     git config user.name "github-actions[bot]"
      #     git config user.email "github-actions[bot]@users.noreply.github.com"
      #     git add followed-users.json
      #     if ! git diff --staged --quiet; then
      #       git commit -m "Update followed users (auto-unfollow)"
      #       git pull --rebase
      #       git push
      #     fi
