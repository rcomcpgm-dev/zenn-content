name: Scheduled Tweets

on:
  schedule:
    # 昼 12:15 JST (UTC 03:15) — 昼休み
    - cron: '15 3 * * *'
    # 夜 19:00 JST (UTC 10:00) — 帰宅〜夕食
    - cron: '0 10 * * *'
    # 夜 22:00 JST (UTC 13:00) — 寝る前
    - cron: '0 13 * * *'
  workflow_dispatch:
    inputs:
      category:
        description: 'Tweet category (tips/mindset/learning/article/devlog/industry)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  tweet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Determine category by time slot
        id: category
        env:
          INPUT_CATEGORY: ${{ github.event.inputs.category }}
        run: |
          HOUR=$(TZ=Asia/Tokyo date +%H)
          if [ "$INPUT_CATEGORY" != "" ]; then
            echo "cat=$INPUT_CATEGORY" >> $GITHUB_OUTPUT
          elif [ "$HOUR" -lt 15 ]; then
            # 昼: Tips, Learning, Article (実用・読み応え系)
            echo "cat=$(shuf -e tips learning article -n 1)" >> $GITHUB_OUTPUT
          elif [ "$HOUR" -lt 20 ]; then
            # 夕方: Industry, Devlog (業界・開発日記系)
            echo "cat=$(shuf -e industry devlog -n 1)" >> $GITHUB_OUTPUT
          else
            # 夜: Mindset, Engagement (共感・交流系)
            echo "cat=$(shuf -e mindset devlog -n 1)" >> $GITHUB_OUTPUT
          fi

      - name: Post tweet
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TWEET_CATEGORY: ${{ steps.category.outputs.cat }}
        run: |
          node scheduled-post.js "$TWEET_CATEGORY"

      - name: Save posted IDs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add posted-ids.json tweet-history.txt
          if ! git diff --staged --quiet; then
            git commit -m "Update posted tweet IDs"
            git pull --rebase
            git push
          fi
